


use the exported client sdk, schema, and types.
failure to use the exported client will cause rejection of components and failure of project milestone.
maintain scope - do not minimize or go beyond without approval.
only modify approved files.  the list of approved files is in the specification.
do not use styles if not explicitly directed to do so.
use components from the current directory.
if a component does not exist then it needs to be imported from another directory, which can only be done by a human user.
