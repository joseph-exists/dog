| Rank | Risk                    | Description                                                   | Likelihood/Impact                                                                               | Mitigation Steps                                                                                                                                                                               |
| ---- | ----------------------- | ------------------------------------------------------------- | ----------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1    | Outbox Starvation       | Workers block on repoleasebusy; jobs stuck retryableerror     | High/Medium (LOCKTTL=60s, multi-worker races) shadow_outbox_worker.py​                          | 1. Add metrics for job status/lease contention. 2. Implement worker health checks + auto-scale. 3. Increase BATCHSIZE=50 under low load. 4. Run repairmissingoutboxjobs cron daily. |
| 2    | Forgejo Outage Cascade  | ApiException 5xx -> all enqueues error/dead after 25 attempts | High/High (no circuit breaker; every save enqueues) ppl-ai-file-upload.s3.amazonaws+1           | 1. Add tenacity/retry with circuit breaker (pybreaker). 2. Health endpoint for Forgejo proxy. 3. Fallback to DB-only mode (disable Forgejo commits). 4. Alert on >10% dead jobs.               |
| 3    | Version Counter Races   | allocatenextversionnumber while True loop under high TPS      | Medium/Medium (IntegrityError handled but CPU/DB spin) shadow_service.py​                       | 1. Use SQL sequence per repo (ALTER SEQUENCE). 2. Shard counters by entitytype. 3. Cap enqueues/sec via semaphore. 4. Monitor loop iterations >1.                                              |
| 4    | Stale Reads in AI Path  | Forgejo read fail -> isstale=True DB fallback; context stale  | Medium/High (summary/context callers unhandled None) ppl-ai-file-upload.s3.amazonaws+1          | 1. Exponential backoff on read retries. 2. Async prefetch in context_loader. 3. Caller-side staleness TTL checks. 4. Dual-write consistency checks post-commit.                                |
| 5    | Unbounded Pending Queue | Enqueue succeeds but no workers -> DB bloat                   | Low/High (best-effort tasks like shadowroomversionbesteffort) ppl-ai-file-upload.s3.amazonaws+1 | 1. Queue length alerts (>1k pending). 2. Auto-repair cron (repairmissing*). 3. Rate-limit enqueues by entitytype. 4. Prune dead jobs >7d.                                                      |