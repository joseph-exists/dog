In agent_runner_streaming.py (lines 83-97), StreamingAgentRunner.run awaits self._get_agent_instance_with_tools( passing: session (AsyncSession, from the method signature), agent_name (str, local alias of req.agent_slug), user_id=req.user_id (uuid.UUID|None, from AgentRunRequest), enable_a2a_tool=req.enable_a2a_tool (bool), enable_ag_ui_tool=req.enable_ag_ui_tool (bool), and room_id=room_id (uuid.UUID, from req.room_id). These types are declared in agent_runner_streaming.py (lines 62-69) (method params) and agent_runner_types.py (lines 9-18) (AgentRunRequest fields).

The _get_agent_instance_with_tools callable is injected via the constructor parameter get_agent_instance_with_tools: Callable[..., Awaitable[Any]] in agent_runner_streaming.py (lines 18-36) and is wired in agent_runner.py (lines 93-100) where _get_streaming_runner() passes get_agent_instance_with_tools imported from app/services/agent_instance.
Return expectation: run immediately treats the awaited value as agent; it logs agent.model if present, checks truthiness, then calls agent.run_stream(...) yielding streamed chunks (agent_runner_streaming.py (lines 99-131)). So the callable is expected to return an awaitable producing an agent-like object (typically a PydanticAI Agent) supporting attributes like model and an async run_stream context manager; None signals instantiation failure.


StreamingAgentRunner.run(req, session) receives req: AgentRunRequest (fields: room_id: uuid.UUID, agent_slug: str, trigger_message: str, optional user_id: uuid.UUID, a2a_depth: int, enable_a2a_tool: bool, enable_ag_ui_tool: bool) and session: AsyncSession — see agent_runner_types.py (lines 8-17).
It calls self._context_service.build(room_id, session, message_limit=20, agent_slug) with room_id: uuid.UUID, session: AsyncSession, message_limit: int, agent_slug: str. The returned context is later passed into _build_agent_prompt; context must support whatever that prompt builder expects (opaque here) — call at agent_runner_streaming.py (lines 82-87).
It awaits self._get_agent_instance_with_tools(session, agent_name, user_id=req.user_id, enable_a2a_tool=req.enable_a2a_tool, enable_ag_ui_tool=req.enable_ag_ui_tool, room_id=room_id) — call site agent_runner_streaming.py (lines 90-97).
Parameter types are implied by the signature documented in agent-instance-trims.md (lines 589-603) (session: AsyncSession, slug: str, user_id: uuid.UUID|None, enable_a2a_tool: bool, enable_ag_ui_tool: bool, room_id: uuid.UUID|None).
The return is awaited; downstream code treats it as an “agent” object that must expose:
attribute model (read for logging, agent_runner_streaming.py (lines 124-128)), and
async context manager method run_stream(prompt, deps, usage_limits) returning a result object with an async iterator stream_text() yielding incremental str chunks (agent_runner_streaming.py (lines 134-148)).
deps = self._deps_factory(session, room_id, agent_name, req.a2a_depth) expects a callable of signature (AsyncSession, uuid.UUID, str, int) -> AgentDeps (wired in agent_runner.py (lines 93-101)). The returned deps object must expose optional ui_components list whose items implement model_dump() (used at agent_runner_streaming.py (lines 150-163)). The provided implementation AgentDeps defines fields session: AsyncSession, room_id: uuid.UUID, current_agent_slug: str, a2a_depth: int, ui_components: list[UIComponent] with a mutable default and .add_ui_component helper (agent_tools.py (lines 23-57)).
_build_agent_prompt(trigger_message, context, current_agent_slug=agent_name) must accept (str, Any, str|None) and return str (type in constructor Callable[[str, Any, str | None], str], agent_runner_streaming.py (lines 35-36)). The returned prompt is passed to agent.run_stream.
get_agent_config(session, agent_name) is awaited to fetch a config that supplies max_tool_iterations int attribute used for UsageLimits(request_limit=…) (agent_runner_streaming.py (lines 114-135)).
agent.run_stream(...) context manager yields result whose stream_text() provides sequential str chunks; full_response accumulates and is later passed to _a2a_orchestrator.process_mentions.
event_publisher.publish_token(room_id: uuid.UUID, agent_name: str, token: str) and event_publisher.emit_message(session: AsyncSession, room_id: uuid.UUID, agent_name: str, content: str, ui_components: list[dict]|None) are awaited; any implementation must support these attributes (agent_runner_streaming.py (lines 143-163)).
_a2a_orchestrator.process_mentions(response: str, responding_agent_slug: str, room_id: uuid.UUID, session: AsyncSession, current_depth: int, run_agent: Callable, user_id: uuid.UUID|None) is awaited; it must return an iterable of dicts containing at least agent_name and success keys, as those are read in the comprehension at agent_runner_streaming.py (lines 181-191).
Wiring: _get_agent_instance_with_tools is injected from app/services/agent_instance by _get_streaming_runner (agent_runner.py (lines 90-103)), so any replacement must preserve the above signature and return-shape expectations.