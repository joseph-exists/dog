in ~dog/:
docker-compose.override.yml
docker-compose.traefik.yml
docker-compose.yml

~/dog/backend/
our core backend for the application.

Dockerfile
and olddf.worker.md as an old attempt that didn't work very well.

~/dog/gittin/
our gogs services - platform repository for repos that are engaged with through the frontend/backend suite.

~/dog/frontend/
Dockerfile
Dockerfile.playwright

~/dog/mcpmvp
no dockerfile?

~/dog/tesser
docker-compose.yml

