# Domain
# This would be set to the production domain with an env var on deployment
# used by Traefik to transmit traffic and aqcuire TLS certificates
DOMAIN=localhost.tiangolo.com
# To test the local Traefik config
# DOMAIN=localhost.tiangolo.com

# Used by the backend to generate links in emails to the frontend
FRONTEND_HOST=http://localhost:5173
# In staging and production, set this env var to the frontend host, e.g.
# FRONTEND_HOST=https://dashboard.example.com

# Environment: local, staging, production
ENVIRONMENT=local

PROJECT_NAME=TinyFoot
STACK_NAME=TinyFootStack

# Backend
BACKEND_CORS_ORIGINS="http://localhost,http://localhost:5173,https://localhost,https://localhost:5173,http://localhost.tiangolo.com"
SECRET_KEY=aG2HXJPfn_6QD1ZXRBJJXH4Lo7WxIP0p4gOIfqYECYI
FIRST_SUPERUSER=joseph@existentialdetectives.com
FIRST_SUPERUSER_PASSWORD=pooppoop

ydc-sk-7b313e3582b794b4-jRULBkVu9HlOREK6Y6KA7CiobU9xKavy-3a01bf05

# Emails
SMTP_HOST=
SMTP_USER=joseph@admin.com
SMTP_PASSWORD=pooppoop
EMAILS_FROM_EMAIL=info@example.com
SMTP_TLS=True
SMTP_SSL=False
SMTP_PORT=587

# Postgres
POSTGRES_SERVER=localhost
POSTGRES_PORT=5432
POSTGRES_DB=tinyfoot
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

SENTRY_DSN=

# Configure these with your own Docker registry images
DOCKER_IMAGE_BACKEND=backend
DOCKER_IMAGE_FRONTEND=frontend

TESSER_REDIS_DB=0
TESSER_REQUEST_CHANNEL=tesser:requests
TESSER_RESPONSE_CHANNEL=tesser:responses
TESSER_WORKER_ID=tesser-worker-1
TESSER_LOG_LEVEL=INFO

# URL template for shadow repos over SSH.
# Must include {entity_type} and {entity_id}, for example:
# SHADOW_REPO_URL_TEMPLATE=ssh://git@gittin/repos/{entity_type}/{entity_id}.git
SHADOW_REPO_URL_TEMPLATE=ssh://git@gittin/repos/{entity_type}/{entity_id}.git
SHADOW_REPO_DEFAULT_BRANCH=main
USER_REPO_GOGS_BASE_URL=http://gittin:3000
USER_REPO_GOGS_ORG=dog
USER_REPO_GOGS_TOKEN=
USER_REPO_GIT_USERNAME=
USER_REPO_GIT_PASSWORD=
