services:
  tesser:
    build:
      context: .
      target: export
    volumes:
      - jobs_data:/data/jobs
      - artifacts_data:/data/artifacts
    restart: unless-stopped

volumes:
  jobs_data:
  artifacts_data:
